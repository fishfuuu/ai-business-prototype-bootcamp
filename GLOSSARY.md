# 主管 AI 原型制作训练营术语词典（GLOSSARY）

本词典是课程的综合术语表，不是考核数据库。每个词条回答“是什么 / 不是什么 / 用意”；三层是知识角色，不是重要程度排序。课程自造框架明确标注。

## 一、主管必须掌握并调用的概念

### AI、Agent 与任务

**大语言模型（LLM）**：
- **是什么**：根据当前上下文推演并生成文字、方案或代码内容的“概率大脑”；需要外部工具和授权才能对真实系统产生影响。
- **不是什么**：全知全能的 AI、自动操作电脑的实体或业务责任人。
- **用意**：判断哪些工作只是生成建议，哪些工作还需要工具、规则和主管批准。

**工具接口（Tools）**：
- **是什么**：系统向 Agent 开放的受控操作通道（如查文件、跑命令、操作浏览器等）。
- **不是什么**：模型天然拥有的能力、业务 API 或无限权限插件。
- **用意**：核对 Agent 实际能做什么以及授权边界在哪里。

**AI Agent**：
- **是什么**：以 LLM 为控制器、以提示和规则为约束、以 Tools 为通道，围绕目标组织多步行动的软件系统。
- **不是什么**：自动化脚本、独立承担业务责任的人或必然可靠的自主决策者。
- **用意**：判断任务是否需要多步观察—行动循环，并明确主管保留的责任。

**Subagent**：
- **是什么**：由主 Agent 调度、在限定任务和上下文中执行子任务的 Agent。
- **不是什么**：另一个随意开启的聊天窗口，也不天然等于只读、隔离或安全。
- **用意**：为子任务单独定义权限、超时、停止条件和产物留存。

**ReAct**：
- **是什么**：Agent 在推理、行动、观察之间循环，把工具结果带回下一步决策。
- **不是什么**：盲目试错、自动改对或无监督自我修复。
- **用意**：识别循环中的观察证据、授权点和停止条件。

**HITL（人在回路）**：
- **是什么**：在人与 AI/Agent 协作的关键节点由人确认、纠正、接管或停止自动化的责任机制。
- **不是什么**：人工客服、只在高风险动作前弹窗，或运行时硬控制。
- **用意**：决定哪里需要人的判断和授权，并保留可解释的责任交接。

**Workflow 与 Agent**：
- **是什么**：Workflow 按预定步骤执行；Agent 可根据目标和观察结果选择下一步。
- **不是什么**：越自主越先进，也不是同一件事的品牌名。
- **用意**：按任务确定性、变化性和错误代价选择范式。

**确定性任务与概率性任务**：
- **是什么**：确定性任务按规则稳定可预测；概率性任务依赖模型判断或生成，结果可能变化。
- **不是什么**：把所有带 AI 的功能都当作概率性，或把模型输出当作事实。
- **用意**：决定规则、模型、验证和人工批准各自承担什么责任。

**AI 介入层级**：
- **是什么**：从不使用 AI、辅助生成、受控 Workflow 到受控 Agent 的介入强度梯度。
- **不是什么**：技术成熟度排行榜或必须逐级升级的路线。
- **用意**：以最低足够自治度解决问题，并保留降级和接管路径。

**AI 机会**：
- **是什么**：业务流程中值得评估是否引入 AI 的具体环节，必须同时考虑任务性质、价值、证据、风险和责任。
- **不是什么**：凡是能接模型的地方，或已经批准实施的需求。
- **用意**：决定哪里值得验证 AI、哪里应明确不使用 AI。

**Agent 范式**：
- **是什么**：围绕一个任务选择的 Workflow、受控 Agent 或人工处理方式及其责任边界。
- **不是什么**：Agent 数量、产品品牌或默认优于 Workflow 的架构。
- **用意**：使自治度与任务变化性、错误代价和责任相匹配。

### 业务、原型与约束

**业务原型**：
- **是什么**：用可操作界面、模拟数据和有限路径表达业务假设的学习与沟通载体。
- **不是什么**：生产系统、完整需求或可直接上线的产品。
- **用意**：低成本定义问题、验证路径并向 IT 交接。

**业务问题边界**：
- **是什么**：使用者、触发、问题、目标和明确非目标共同限定本次业务范围。
- **不是什么**：一句话需求或全部部门需求。
- **用意**：控制原型范围并为契约、验收和责任划界。

**业务契约**：
- **是什么**：把业务目标、角色、流程、规则、边界和通过条件写成可核对的约定。
- **不是什么**：代码实现、技术架构或模糊需求。
- **用意**：让主管和 IT 对“做什么、何时算成”使用同一语言。

**数据契约**：
- **是什么**：字段、类型、枚举、来源和缺失行为等数据结构约定。
- **不是什么**：随机假数据、TypeScript 类型本身或真实生产数据。
- **用意**：建立界面与未来 API 之间的可追溯边界。

**验收场景**：
- **是什么**：包含前置条件、操作、预期观察、通过标准和失败处置的可重复业务情境。
- **不是什么**：只写“能用”的口头检查或单纯技术测试。
- **用意**：把业务验收转成可执行、可留证的判断。

**Skill**：
- **是什么**：可复用的打包指令（`SKILL.md`），Agent 装载后按它执行某种固定方法。
- **不是什么**：一次性 Prompt，或 Agent 本身、某个工具。
- **用意**：把一套方法固化、跨项目重用，区别于临时写的一次性指令。

**薄切片（纵向切片）**：
- **是什么**：足够小而能独立验证、同时从输入到结果完整跑通一条业务路径的“纵向最小闭环”。
- **不是什么**：技术碎片或一次完成整个系统。
- **用意**：控制实施风险，让每次改变都能被主管验收。

**项目级 Agent 约束 / 行为护栏（Agent Harness）**：
- **是什么**：项目给 Agent 提前立好的规矩与底线（包括工作方式与界面设计规范），防止 AI 越界或随意发挥。
- **不是什么**：单次提示词、运行时硬控制或保证 Agent 必然遵守的魔法层。
- **用意**：让 Agent 跨课工作时可检索、可核对、可追责。

**约束作用域与优先级**：
- **是什么**：规则按“项目级 > 目录级 > 单文件级”逐级生效；全局红线高于局部规则，任何单个文件都不能擅自突破项目底线。
- **不是什么**：所有规则同层，或局部文字可以覆盖全局安全红线。
- **用意**：判断规则是否适用于当前行动及冲突如何处理。

**行为约束与设计约束**：
- **是什么**：行为约束规定 Agent 如何工作；设计约束规定产物应满足的视觉、交互和信息层级。
- **不是什么**：代码级强制或针对单页的口头偏好。
- **用意**：分别检查过程安全和结果质量。

**信息优先级与视觉层级**：
- **是什么**：按业务任务重要性安排内容、状态、操作和视觉强调的关系。
- **不是什么**：单纯配色偏好或“高端大气”的形容词。
- **用意**：把参考图和视觉选择映射回业务目标。

**约束一致性验证**：
- **是什么**：检查实现是否满足项目约束、设计约束和验收场景，并留下对比证据。
- **不是什么**：只看文件存在或凭主观感觉说“像了”。
- **用意**：在批准视觉重构或交接前发现约束遗漏。

**设计令牌（Design Token）**：
- **是什么**：可复用的颜色、字号、间距、圆角等视觉变量。
- **不是什么**：随机内联样式或完整设计约束机制。
- **用意**：保持视觉一致并便于 IT 接续实现。

**本地预览**：
- **是什么**：在本机开发服务器中观察原型外观和交互的环境。
- **不是什么**：生产环境或测试环境；“试衣镜”只能作为首次解释后的教学比喻，不是正式术语。
- **用意**：快速观察原型变化并执行低成本验收。

**工作区 / Working Tree**：
- **是什么**：当前正在修改、尚未必提交到 Git 历史的文件状态。
- **不是什么**：缓存目录或天然可恢复的备份。
- **用意**：识别候选改动与已确认版本的边界。

### 控制、证据与数据

**版本证据与回退**：
- **是什么**：用 Working Tree、Diff、Commit 记录变更、比较差异和恢复已保存版本。
- **不是什么**：业务审批系统，也不要求主管手敲危险命令。
- **用意**：为验收和交接提供可定位的版本证据。

**技术状态与业务状态**：
- **是什么**：技术状态描述文件或程序是否完成；业务状态描述目标、规则和责任是否被主管确认。
- **不是什么**：页面能打开就等于业务通过。
- **用意**：避免把 Agent 自称完成误当业务验收。

**可追溯验收**：
- **是什么**：把验收场景、操作、观察、原始证据、结论和责任关联到具体版本。
- **不是什么**：口头验收、只存截图或把测试通过当业务批准。
- **用意**：让 IT 能复核主管批准了什么。

**事实、假设与生成错误**：
- **是什么**：事实有可核对证据；假设是待验证判断；生成错误是不实或不合约的模型产出。
- **不是什么**：把模型说得流畅当事实，或把所有失败归咎于网络。
- **用意**：决定继续验证、修复、停止还是升级。

**根因与表象**：
- **是什么**：表象是可见症状，根因是造成症状的更深层原因。
- **不是什么**：修掉错误文字就代表问题已解决。
- **用意**：避免围绕表象反复重试。

**有界排错**：
- **是什么**：限定主假设、修改范围和重试预算。
- **不是什么**：无限试错。
- **用意**：失败时保留证据并停止或升级。

**证据链**：
- **是什么**：把场景、操作、观察、原始证据和结论串成可复核记录。
- **不是什么**：无落盘物证的口头结论或“文件存在”证明业务通过。
- **用意**：支持验收、独立评审和责任交接。

**自动化验证与业务验收**：
- **是什么**：自动化验证检查技术事实；业务验收由主管判断目标是否满足。
- **不是什么**：自动化测试通过就等于主管批准。
- **用意**：分开工具能证明的事实与主管必须承担的判断。

**提示注入与不可信输入**：
- **是什么**：外部文本夹带误导指令，试图让 Agent 越过授权边界。
- **不是什么**：普通输入、模型幻觉或业务 Bug 的同义词。
- **用意**：把页面、消息和数据当作待验证内容，而不是自动授权。

**护栏（Guardrail）**：
- **是什么**：文字指导、流程门禁或运行时硬控制的约束统称。
- **不是什么**：某一层就能替代其他层，也不是仅靠文字即可强制执行。
- **用意**：按风险选择人、流程和代码的控制强度。

**工作流门禁**：
- **是什么**：靠流程检查、批准或停止来控制行动的节点。
- **不是什么**：代码拦截或自动批准。
- **用意**：明确主管在哪些阶段必须作出裁决。

**运行时硬控制**：
- **是什么**：由代码或系统在运行时强制拒绝不允许的动作。
- **不是什么**：Markdown 规则、口头约定或流程提醒。
- **用意**：识别哪些红线不能只靠 Agent 记住。

**停止 / 升级 / 恢复**：
- **是什么**：停止当前自动化、交给更高责任层、或回到已确认版本的处置动作。
- **不是什么**：失败后无限重试或未经批准的自动回滚。
- **用意**：在证据不足或风险上升时保持可控。

**重试预算 / 熔断**：
- **是什么**：为自动重试设定次数、时间和风险上限，达到阈值就停止。
- **不是什么**：保证恢复成功的机制或普遍“两轮提交”。
- **用意**：防止 Agent 在错误路径中无限循环。

**兜底降级（Fallback）与失败责任**：
- **是什么**：当 AI 无法给出可信结果时，必须明确标注降级来源（缓存/规则/人工兜底）与第一责任人，绝不能用假数据冒充真实成功。
- **不是什么**：隐藏失败或一句“AI 出错了”就结束责任。
- **用意**：让业务判断结果可信度和下一步责任。

**数据分级与脱敏**：
- **是什么**：按敏感等级决定数据能否进入 AI 和材料，并在进入前完成脱敏。
- **不是什么**：事后补救、加密传输或单纯权限管理。
- **用意**：防止真实敏感数据进入对话或证据材料。

**Mock Data**：
- **是什么**：符合契约但完全虚构的数据，用于原型演示和安全验证。
- **不是什么**：生产数据、未脱敏真实数据或默认可信来源。
- **用意**：在不接真实系统时验证界面和业务路径。

### 接入、协作与交接

**MCP 最小模型**：
- **是什么**：Host 通过 Client 连接 MCP Server；Server 暴露可调用的 Tool。Tool 是 Server 提供的能力，不与 Host/Client/Server 并列为同类角色。
- **不是什么**：业务 API、模型 API 或学员必须配置的服务。
- **用意**：区分工具接入层与业务能力层，追踪调用、凭证和失败责任。

**开发工具链**：
- **是什么**：用于构建和验证的本地工具，如 npm、脚本和浏览器工具。
- **不是什么**：业务 API 或模型供应商服务。
- **用意**：识别工具故障、凭证和责任 owner。

**业务 API**：
- **是什么**：业务系统提供的产品能力接口。
- **不是什么**：模型 API、MCP 或本地开发工具。
- **用意**：判断业务数据和业务责任由哪个系统承担。

**模型 API**：
- **是什么**：模型供应商提供的生成或推理服务接口。
- **不是什么**：业务系统接口或本地工具。
- **用意**：评估模型服务的成本、失败、数据和责任边界。

**三链区分**：
- **是什么**：分别追踪开发工具链、业务 API、模型 API 的调用者、凭证、数据、失败和 owner 的框架。
- **不是什么**：把三条链混称，或把 MCP 当成业务链。
- **用意**：避免部门 AI 机会和架构选型中的责任错配。

**上下文隔离（双盲独立评审）**：
- **是什么**：让独立评审只接收最终产物与客观证据，坚决不带入开发过程中的聊天记录与自我辩护。
- **不是什么**：简单开新窗口或让评审者修改实现。
- **用意**：降低自我辩护、提示污染和越权读取风险。

**独立 Finding（审查发现项）**：
- **是什么**：独立评审模型基于证据指出的具体问题、业务影响与处置建议。
- **不是什么**：泛泛意见、实现者自评或无证据偏好。
- **用意**：让主管逐项决定接受、修复、停止或延期。

**验证、评审与拍板三权分立**：
- **是什么**：执行者自验（自己做对了吗）、独立模型挑刺（第三方看有没有问题）、业务主管拍板（决定接不接受现状）；三权分立，责任各司其职。
- **不是什么**：测试通过自动等于评审通过或主管批准。
- **用意**：避免把技术完成误当业务确权。

**独立评审**：
- **是什么**：隔离上下文中由未参与实现的评审者检查规格、证据和变更。
- **不是什么**：实现者审查自己，或把批准责任交给评审者。
- **用意**：发现实现者遗漏的范围、术语和风险问题。

**主管裁决**：
- **是什么**：主管基于 Finding、证据和业务影响作出的最终处置判断。
- **不是什么**：Agent 自裁或无证据拍板。
- **用意**：保留业务责任与取舍权。

**原型与生产系统**：
- **是什么**：原型验证假设和交接；生产系统还需安全、数据、集成、监控、成本和合规。
- **不是什么**：原型能跑就可以上线。
- **用意**：明确课程交付边界和生产化缺口。

**风险红线与非目标**：
- **是什么**：明确不可做的高风险行为和本次不解决的范围。
- **不是什么**：事后提醒或把未来需求算进验收。
- **用意**：帮助主管停止越界工作并交接未完成事项。

**产品决策**：
- **是什么**：基于业务价值、证据、风险和成本，并明确责任，对继续、调整、暂缓或停止作出的业务决定。
- **不是什么**：技术实现计划或上线批准。
- **用意**：把原型学习结果转成组织可执行方向。

**生产就绪缺口（工程落地差距清单）**：
- **是什么**：原型到正式生产上线之间仍需 IT 补齐的安全、真实数据、高并发、监控和合规等差距清单。
- **不是什么**：原型 Bug 清单或生产批准。
- **用意**：让 IT 明确下一阶段工作。

**责任交接**：
- **是什么**：把范围、契约、证据、红线、未决项和责任 owner 明确交给 IT 或后续团队。
- **不是什么**：把文件丢进共享目录或一句“请继续开发”。
- **用意**：使交接可接续、可追责、可复核。

## 二、操作方法

**结构化追问（Grill-Me）**：**是什么**：按问题、目标、边界、风险、通过条件和停止条件追问并固化业务契约。**不是什么**：一次模糊提问。**用意**：减少需求脑补。

**多模态参考提取**：**是什么**：从参考图、文字和页面观察中提取可验证的结构、层级和约束。**不是什么**：像素照抄。**用意**：提取可迁移意图。

**设计约束映射**：**是什么**：把业务目标映射为信息层级、组件、状态和设计令牌规则。**不是什么**：直接写 CSS。**用意**：让视觉选择可核对。

**Keep / Omit / Modify**：**是什么**：逐条保留、删除或修改参考元素，并写明业务理由。**不是什么**：全盘照搬。**用意**：明确视觉裁决。

**读取—映射—执行—核对**：**是什么**：先读取约束，再映射到任务，执行授权范围，最后按证据核对。**不是什么**：文字硬控制。**用意**：降低 Agent 越界。

**视觉证据对比**：**是什么**：用前后截图、观察记录和约束清单比较变化。**不是什么**：主观说“更好看”。**用意**：证明视觉变化有理由。

**最小业务路径确认**：**是什么**：先确认一条从输入到结果的最短可操作路径。**不是什么**：先做全系统。**用意**：快速验证价值。

**Plan & Execute**：**是什么**：把工作拆成带路径、边界、状态和验收信号的可验证增量。**不是什么**：一次性生成全部功能。**用意**：控制实施风险。

**两阶段版本记录（本课程方法）**：**是什么**：分别记录候选实现和主管业务处置。**不是什么**：Git 强制规则，也不要求两个 Commit。**用意**：区分技术变化与业务决定。

**事实锚定排错**：**是什么**：先记录可复现事实和 PASS/FAIL 信号，再提出假设、最小修改并复验。**不是什么**：盲改。**用意**：让修复可验证。

**五层诊断**：**是什么**：按环境、数据源、组件状态、日志和契约断言逐层定位。**不是什么**：同时乱查所有层。**用意**：缩小根因范围。

**有界恢复**：**是什么**：由有权限人员按确认范围恢复到已知版本。**不是什么**：未经批准的自动回滚。**用意**：记录责任与证据。

**浏览器自动化验收**：**是什么**：用预定义场景、操作和停止条件自动采集可观察证据。**不是什么**：自动批准。**用意**：把证据交主管做业务验收。

**验收场景迁移**：**是什么**：把业务验收场景转换成自动化步骤。**不是什么**：重新发明验收标准。**用意**：保留业务含义并便于自动执行。

**Subagent 课程安全规程**：**是什么**：为子 Agent 单独设只读优先、隔离上下文、超时停止、授权边界和磁盘落盘。**不是什么**：Subagent 的行业本体定义。**用意**：降低课程实操风险。

**Agent 协作模式选型**：**是什么**：按依赖、并行性、独立性和责任选择协作方式。**不是什么**：行业唯一标准。**用意**：选择最简单足够的协作。

**accept / fix / stop / defer**：**是什么**：主管逐项接受、修复后重验、停止功能或延期 Finding。**不是什么**：Agent 自动处置。**用意**：写下业务理由。

**AI 机会地图**：**是什么**：按业务环节标注问题、介入层级、范式、风险、证据和 owner，也标出不做 AI 的位置。**不是什么**：强行找 AI 的清单。**用意**：支持部门级筛选。

**介入层级迁移**：**是什么**：把单点智能薄片的判断标准迁移到部门流程其他环节。**不是什么**：自动复制方案。**用意**：保持判断一致。

**Agent 范式选型**：**是什么**：基于任务性质、错误代价和责任边界选择 Workflow、Agent 或人工处理。**不是什么**：默认选择 Agent。**用意**：匹配责任与自治度；它是“Agent 范式”的操作方法。

**原型交付就绪度检查（课程框架）**：**是什么**：检查证据、红线、未决缺口和责任交接。**不是什么**：CMM/CMMI。**用意**：判断是否适合交给 IT。

**四步概念解析公式（教师方法）**：**是什么**：用工程定义、运作机制、当课操作例子与边界、交接价值讲解首次核心概念；定义已经能讲清时不强行添加类比，确需类比时只保留一套稳定映射。**不是什么**：主管必须背诵的行业标准。**用意**：帮助教师建立可迁移理解；术语定义以本词典为准，课程行为与限制以《十课冻结基线》为准。

## 三、交付产物

**AGENTS.md / CLAUDE.md**：**是什么**：项目级 Agent 行为指令与约束文件。**不是什么**：运行时硬控制。**用意**：让行为边界可检索、可复核。

**DESIGN.md（或同等视觉约束文件）**：**是什么**：承载设计约束、信息层级和视觉令牌的项目文件。**不是什么**：口头审美要求或运行时拦截器。**用意**：让 Agent 和 IT 使用同一视觉约束。

**个人业务原型**：**是什么**：学员持续迭代的可操作原型及跨课状态记录。**不是什么**：生产系统。**用意**：持续验证业务路径。

**业务功能卡**：**是什么**：记录角色、问题、目标、边界、流程和验收重点的业务材料。**不是什么**：完整技术设计。**用意**：给契约和交接提供业务起点。

**数据契约卡 / Mock 样例**：**是什么**：可核对的数据结构约定与标记清晰的虚构样例。**不是什么**：真实生产数据。**用意**：安全验证字段和界面。

**验收场景卡**：**是什么**：可执行的前置条件、操作、观察、通过标准和失败处置记录。**不是什么**：一句“能用”。**用意**：支持重复验收。

**薄切片候选**：**是什么**：待实施的最小完整业务路径及验证信号。**不是什么**：技术碎片。**用意**：控制实施范围。

**版本证据**：**是什么**：与 Working Tree、Diff 或 Commit 关联的变更和处置记录。**不是什么**：业务批准本身。**用意**：追溯实际变化。

**智能机会卡 / 原型智能薄片**：**是什么**：候选智能环节的目标、介入层级、范式、风险、降级和验收证据。**不是什么**：上线承诺。**用意**：约束一次智能试验。

**诊断报告**：**是什么**：事实、假设、根因判断、尝试、验证信号和停止/升级/恢复决定的记录。**不是什么**：AI 自称修复的结论。**用意**：支持有界排错。

**浏览器验收卡 / 证据索引**：**是什么**：自动化操作、截图、日志、观察与结论的可定位索引。**不是什么**：自动批准。**用意**：让证据可复核。

**独立评审报告**：**是什么**：隔离评审输入、独立 Finding、严重性、证据和建议处置的报告。**不是什么**：实现者自评。**用意**：支持主管裁决。

**主管处置卡**：**是什么**：主管对每个 Finding 作出的 accept/fix/stop/defer 决定及理由。**不是什么**：Agent 自动批准。**用意**：留存业务责任。

**Agent 选型图**：**是什么**：部门流程环节、任务性质、介入层级、Agent 范式、Fallback 和 owner 的图示。**不是什么**：自动选型器。**用意**：支持部门级决策。

**产品决策记录**：**是什么**：记录继续、调整、暂缓或停止及其证据、风险和责任的交付材料。**不是什么**：产品决策概念本身或上线批准。**用意**：把产品决策留痕并形成组织可复核的交接材料。

**开发启动包**：**是什么**：供 IT 正式设计与开发的范围、契约、原型、证据、红线、缺口和责任材料集合。**不是什么**：生产系统。**用意**：让 IT 可接续工作。

**业务异常预警与闭环处置工作台**：**是什么**：教师统一案例产物，覆盖看指标、看异常、下钻明细和决策闭环。**不是什么**：通用行业概念。**用意**：贯穿十课演示。
