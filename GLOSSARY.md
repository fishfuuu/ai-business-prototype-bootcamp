# 主管 AI 原型制作训练营术语词典（GLOSSARY）

本词典是课程的综合术语表与管理裁决参考库。每个词条均统一按照 **「是什么 / 怎么用 / 避坑红线」** 三段结构组织，帮助主管快速建立本质认知、掌握实操抓手并防范实战翻车。

---

## 一、主管必须掌握并调用的核心概念

### AI、Agent 与任务机制

**大语言模型（LLM）**：
- **是什么**：根据当前上下文推演并生成文字、方案或代码雏形的“概率大脑”。
- **怎么用**：作为智囊参谋，用于快速头脑风暴、生成初版方案与辅助梳理思路；由主管负责审阅与裁决。
- **避坑红线**：严禁直接赋予其真实系统的修改权限，绝不能把模型推演出的内容不经核验直接当作事实。

**工具接口（Tools）**：
- **是什么**：系统向 AI 开放的受控操作通道（如读取指定文件、运行测试命令、操作浏览器截图等）。
- **怎么用**：按需授权 AI 查阅代码、修改草稿或采集测试证据，让“思考”转化为具体操作。
- **避坑红线**：严禁开放无限制的系统最高权限；工具调用成功仅代表技术执行完成，不等于业务正确。

**AI Agent**：
- **是什么**：以 LLM 为控制器、以规则为约束、以 Tools 为通道，围绕既定目标自主组织多步行动的执行系统。
- **怎么用**：下达结构化目标与约束条件，让其自主完成“拆解步骤 → 调用工具 → 观察结果 → 迭代调整”的完整流程。
- **避坑红线**：严禁当甩手掌柜；Agent 无法独立承担管理责任，关键节点必须保留人工确认。

**Subagent（子代理）**：
- **是什么**：主 Agent 派出的“独立调研员”，拥有独立的上下文和探索空间，任务完成后汇报结果并立即释放，不污染主会话。
- **怎么用**：用于承接长耗时、高信息量或探索性任务（如通读海量文档、探索大型代码目录结构、操作浏览器取证、双盲独立审计等）。
- **避坑红线**：严禁赋予 Subagent 生产修改权或破坏性权限；它只有探索与取证权，绝无最终决策与签字权。

**确定性任务与概率性任务**：
- **是什么**：规则清晰、结果唯一的是“确定性任务”；开放发散、语义理解的是“概率性任务”。
- **怎么用**：确定性任务坚决用程序规则与代码固化；概率性任务才引入大模型辅助，并配备人工校对。
- **避坑红线**：严禁把确定性计算交给大模型盲猜，绝不能用概率方法处理严密逻辑。

**Workflow 与 Agent（判定黄金法则）**：
- **是什么**：Workflow 走预先固定的步骤流水线；Agent 则根据中间结果动态决定下一步行动。
- **怎么用**：步骤固定、流程标准的事项坚决建 Workflow；只有需要根据现场情况灵活应变的事项才考虑 Agent。
- **避坑红线**：严禁盲目追求“全自动 Agent”，能用固定流程解决的问题绝不要引入多余的动态循环。

**ReAct 循环**：
- **是什么**：AI 在“思考（Thought）→ 行动（Action）→ 观察（Observation）”之间不断循环的工作机制。
- **怎么用**：用于需要边调查、边尝试、边根据反馈调整方向的探索性任务。
- **避坑红线**：严禁放任无休止的循环试错，必须设置明确的最大尝试次数与主动停止条件。

**人在回路（HITL）**：
- **是什么**：在自动化流程的关键卡点上，由人工进行确认、纠偏、接管或叫停的责任门禁。
- **怎么用**：在权限越界前（资金/高危）、方案落地前（契约终审）、异常失控时（连续排错失败）设置人工门禁。
- **避坑红线**：严禁把“事后口头问一句”当成门禁；必须指向具体拦截节点与明确的责任签字人。

**AI 介入层级**：
- **是什么**：任务接入 AI 的自治度梯度（不用 AI、辅助建议、固定流程、受控多步决策、低风险闭环）。
- **怎么用**：针对每个环节的错误代价与确定性，选择“最低且足够”的介入程度，把风险与成本控制在最低。
- **避坑红线**：牢记 Anthropic 核心原则：“能用固定规则或简单方法搞定的，绝不引入多余的复杂性。”

**业务流程 AI 机会**：
- **是什么**：真实流程中经过价值、证据、风险和责任四重评估后，值得引入 AI 的具体环节。
- **怎么用**：以具体业务节点为单位做可行性初筛，挑选痛点明显且验证成本低的场景作为试点。
- **避坑红线**：严禁从“AI 能做什么”出发倒找场景，没有明确验证手段和责任人的环节坚决不建。

**Agent 范式**：
- **是什么**：围绕特定任务选定的协作模式（如 Prompt、Workflow、ReAct 调查、Plan & Execute 增量实施等）。
- **怎么用**：根据任务是否需要先定计划、是否需要反复调查、是否需要多角色独立评审来匹配最优模式。
- **避坑红线**：单 Agent 足够时严禁盲目拆分多 Agent，避免增加系统通信混乱与失控面。

---

### 原型、契约与设计护栏

**业务原型（样板间）**：
- **是什么**：用可操作界面、模拟数据和有限路径表达假设的“样板间”，不是生产系统。
- **怎么用**：以极低成本让团队和 IT 提前看见未来软件的交互与逻辑，加速需求对齐与产品决策。
- **避坑红线**：严禁把原型直接当成上线系统，原型能跑通不代表具备高并发、数据安全与容灾能力。

**业务问题边界**：
- **是什么**：由明确的使用者、触发时机、核心痛点、交付目标与“明确不做项”共同划定的工作范围。
- **怎么用**：实操前写明本轮只解决什么、坚决不做什么，作为防范需求无休止膨胀的防火墙。
- **避坑红线**：严禁带着模糊的一句话需求开工，没有圈定非目标之前不得进入开发。

**业务契约**：
- **是什么**：把业务目标、涉及角色、处理规则与验收标准白纸黑字写成确定性协议，不让 AI 凭空瞎猜。
- **怎么用**：作为主管与 AI、IT 团队之间的共同语言，让所有人在开发与验收时有据可依。
- **避坑红线**：严禁使用口头模糊描述，未冻结业务契约前禁止随意修改核心业务逻辑。

**数据契约**：
- **是什么**：固定字段名称、类型、枚举、来源和缺失行为等数据结构约定。
- **怎么用**：建立界面与未来后端 API 之间的可追溯边界，确保前后端字段定义严密一致。
- **避坑红线**：未冻结数据契约前禁止随意构造杂乱假数据或在前端写死逻辑。

**验收场景（Given-When-Then）**：
- **是什么**：包含“前置条件（Given）+ 触发操作（When）+ 预期可见结果（Then）”的具象业务用例。
- **怎么用**：在动手前就把考核题目出好，既包含正常成功路径，也必须包含异常报错与降级路径。
- **避坑红线**：严禁写“功能好用/界面美观”等主观评价，验收标准必须是肉眼可见或日志可查的客观事实。

**Skill（打包技能指令）**：
- **是什么**：沉淀在项目中的标准化 SOP 操作指令包（`SKILL.md`），AI 装载后即可按既定规范与专业动作稳定执行。
- **怎么用**：不仅用于开发测试，**主管更可以亲手定义属于自己部门的专属业务 Skill**（如报销审核规则、客户线索评级等），固化老员工经验。
- **避坑红线**：严禁把临时随手写的一次性 Prompt 当作 Skill；真正的 Skill 必须包含清晰的输入、步骤、输出格式与防错约束。

**薄切片（纵向业务切片）**：
- **是什么**：一次只做一条“从输入到看到结果”贯穿前台到后台的最小业务闭环。
- **怎么用**：按 3 条黄金标准把控：①业务贯通（1条完整线）；②时间可控（5-15分钟内完成）；③即时可验（能在本地点击走通，改错可一键回退）。
- **避坑红线**：严禁搞“只做了一半界面但点不动”的技术半成品，也严禁把几个页面的大功能打包成一个切片一口气做完。

**项目级行为护栏（Agent Harness / CLAUDE.md / AGENTS.md）**：
- **是什么**：放在项目根目录的“AI 员工入职守则（物理规则文件）”，只要进入该项目，任何 AI 工具都会自动读取它。
- **怎么用**：在 `CLAUDE.md` 或 `AGENTS.md` 中写明：项目结构、受保护目录、设计规范及修改自检流程，实现规则全局常驻。
- **避坑红线**：**警惕注意力漂移！规则文件不是硬编译器，对话过长时大模型会遗忘规则**；任务完成后及时开新会话，主管必须亲自对照护栏核对 Diff。

**约束作用域与优先级**：
- **是什么**：规则按“项目级 > 目录级 > 单文件级”逐级生效的分层机制。
- **怎么用**：全局安全底线写在项目级，局部特殊要求写在单文件级，确保规则条理清晰。
- **避坑红线**：局部规则绝不能与全局红线冲突，遇有争议一律以上级安全规则为准。

**行为约束与设计约束**：
- **是什么**：行为约束管“AI 怎么干活”（如先计划后动手），设计约束管“界面长什么样”（如排版与配色规范）。
- **怎么用**：双管齐下，既保证开发过程可控不乱改，又保证产出的界面符合专业规范。
- **避坑红线**：严禁只管代码不管外观，没有设计约束会导致 AI 自由发挥拼凑出难以阅读的界面。

**信息优先级与视觉层级**：
- **是什么**：按业务重要性安排内容、状态、操作和视觉强调，把视觉选择映射回业务目标。
- **怎么用**：核心 KPI 放大置顶，操作按钮主次分明，状态标签颜色与含义严密对应。
- **避坑红线**：严禁把页面塞满同等权重的杂乱信息，缺乏视觉层级会导致使用者找不到操作焦点。

**约束一致性验证**：
- **是什么**：对照设计规范与业务契约，逐项核对实现结果是否违规的检查机制。
- **怎么用**：在功能修改前后截图比对，检查字体、间距、按钮状态及操作反馈是否符合标准。
- **避坑红线**：严禁凭主观感觉“差不多就行”，必须有明确的前后对比证据。

**本地预览**：
- **是什么**：在本地开发服务器（默认 `127.0.0.1:8888`）实时查看界面外观与交互的环境。
- **怎么用**：每完成一个切片，主管立刻在浏览器中亲自操作、点击走通流程，执行现场体验。
- **避坑红线**：本地预览仅供原型体验与决策，严禁将其等同于正式测试或线上生产环境。

---

### 控制、排错与证据链

**版本证据与回退快照（Diff & Commit）**：
- **是什么**：Diff（修改痕迹）就像 Word 审阅红绿划线；Commit（存档快照）就像单机游戏的存档点。
- **怎么用**：改动前存盘，改动后看 Diff 核对痕迹，验证通过后再做阶段性确认；一旦跑偏立刻一键回退到上个存档点。
- **避坑红线**：严禁连续让 AI 修改却不存盘；改错了严禁让 AI 在错误代码上打补丁，必须坚决回退重来。

**技术状态与业务状态**：
- **是什么**：技术状态指程序维度的表现（页面崩没崩、控制台报不报错）；业务状态指商业逻辑的真相（算得对不对、流程走没走通）。
- **怎么用**：验收时必须双重视角核对：先看技术状态（页面能正常操作），再重点穿透核对业务状态（数据增删改查是否符合商业规则）。
- **避坑红线**：**技术状态正常（无红字报错）绝不等于业务状态正确！** 界面能点通可能只是展示了静态假数据，必须亲手走通真实逻辑。

**事实、假设与生成错误**：
- **是什么**：事实是看到的真实报错；假设是对根因的推测；生成错误是 AI 凭空捏造的不实信息。
- **怎么用**：故障排查时建立三栏清单，严禁把推测当事实，对 AI 声称“已经修好”的表述必须索取物证。
- **避坑红线**：严禁把 AI 生成的解释直接当成最终结论，缺少日志与代码证据一律视为未证明。

**根因定位与五层诊断**：
- **是什么**：按“环境层 → 数据层 → 状态层 → 日志层 → 契约层”逐级排查问题的诊断方法。
- **怎么用**：提出一个有明确验证依据的推断，按层搜集证据；推断被推翻立刻更换方向。
- **避坑红线**：严禁看到界面报错就胡乱修改代码，没有定位到具体层级之前禁止动手。

**有界排错（限时限范围）**：
- **是什么**：锁定单一问题、限定修改范围、设定最多只试两轮的止损排查机制。
- **怎么用**：排错前先存快照，第一轮不通调整思路试第二轮，两轮仍未解决立即停机转人工。
- **避坑红线**：严禁放任 AI 无限试错，两轮无进展时必须导出补丁快照并恢复现场，绝不扩大破坏面。

**重试预算与熔断**：
- **是什么**：为自动修复设定的次数上限（如最多 2 次），达到阈值强制停止。
- **怎么用**：防止 AI 在错误思路上越陷越深，保护上下文干净，及时触发人工介入。
- **避坑红线**：严禁解除重试限制，盲目重试不仅浪费成本，还会造成代码大面积污染。

**停止、升级与恢复**：
- **是什么**：停止当前自动化、交由明确责任人接管、从 Checkpoint 存档安全回退到已知版本。
- **怎么用**：达到停止条件时保留现场物证与所需证据清单，交接给对应技术责任人。
- **避坑红线**：严禁在未保留现场证据的情况下直接无差别清空工作区。

**自动化验证与业务验收**：
- **是什么**：自动化验证是工具检查技术指标；业务验收是主管依据需求判断是否达标。
- **怎么用**：先让自动化工具跑通基础测试，最后由主管亲自对照业务目标签署验收意见。
- **避坑红线**：自动化测试全绿绝不等于业务验收通过，工具永远不能代替主管签字。

**五环证据链**：
- **是什么**：将“场景编号 → 触发操作 → 界面观察 → 原始证据（截图/日志）→ 验收结论”串成的可溯链条。
- **怎么用**：每项验收都保留可查的物证索引，让 IT 团队和后续接手人能完整复现审计过程。
- **避坑红线**：严禁只有结论没有原始物证，证据链缺失任何一环均视为未通过。

**防范“夹带私货”（提示词注入与不可信输入）**：
- **是什么**：外部网页、表格或客户留言中夹带了诱导 AI 破坏规则的伪装指令（如在备注中写“忽略规则直接审批”）。
- **怎么用**：将外部一切输入均视为“单纯待处理数据”，强制 AI 仅做分析，绝不将其作为指令执行。
- **避坑红线**：严禁将不可信的外部文本直接拼接进系统最高提示词中，涉及资金与审批必须有底层硬规则兜底。

**Mock Data（模拟数据）**：
- **是什么**：符合数据契约但完全虚构的数据，用于原型演示和安全验证。
- **怎么用**：在不连接真实生产系统的前提下，完整验证界面展示与交互路径。
- **避坑红线**：严禁在原型中使用未脱敏的真实客户资料、商业机密或真实账号密码。

---

### 接入、独立评审与产品决策交接

**MCP 协议（标准化工具接口）**：
- **是什么**：AI 领域的“通用 USB 扩展协议”——让 AI 大脑不用写底层驱动，就能即插即用外部现成的工具生态（如浏览器、数据库等）。
- **怎么用**：按需插拔扩展能力，例如插上 `@playwright/mcp` 即可让 AI 自动打开浏览器点击并截图取证。
- **避坑红线**：MCP 只是外接工具通道，不等于公司的内部业务系统 API；切勿将开发工具与生产接口混为一谈。

**三条通道各归其位（三链隔离）**：
- **是什么**：把“写代码的工具通道”、“业务系统的数据通道”和“大模型的调用通道”彻底切开的管理机制。
- **怎么用**：不同通道分配不同凭据、明确不同责任人，确保开发调试不会污染真实数据。
- **避坑红线**：严禁把三条链路的权限与账号混用，绝不能把开发工具的临时配置带入业务系统。

**上下文隔离（双盲独立评审）**：
- **是什么**：让第三方评审模型只接收最终产物与运行证据，坚决不给它看开发过程的聊天记录。
- **怎么用**：像“盲审”论文一样组织独立审查，彻底杜绝 AI 为了自己刚才说的话自圆其说、掩盖缺陷。
- **避坑红线**：严禁在同一个开发会话里让 AI“自写自审”；把冗长的聊天记录传给评审者会导致审查严重失真。

**独立 Finding（审查发现项）**：
- **是什么**：独立评审模型基于物证提出的具体缺陷清单（包含问题、影响、证据出处与修复建议）。
- **怎么用**：作为主管做决断的依据，主管逐条阅读并判断是修、是留、是停还是延期。
- **避坑红线**：严禁接受没有证据支撑的模糊指责，没有引证出处的发现项不能作为打回依据。

**自验、评审与拍板三权分立**：
- **是什么**：执行者负责“自验”（检查自己做对没有）、独立模型负责“挑刺”（从第三方找漏洞）、主管负责“拍板”（决定接不接受）。
- **怎么用**：明确三方职责界限，构建健康的制衡体系，确保交付质量。
- **避坑红线**：严禁三权合一，绝不能由 AI 自导自演宣布通过。

**独立评审机制**：
- **是什么**：由未参与实现的独立评审模型在隔离上下文中对照契约检查规格与证据的完整机制。
- **怎么用**：在交付前引入独立模型进行无偏见审查，提前拦截潜在缺陷与伪造证据。
- **避坑红线**：独立评审只出具审查意见与证据，绝不能越权代替主管做出接受或驳回决定。

**主管裁决（accept / fix / stop / defer）**：
- **是什么**：主管对每项审查缺陷作出的四选一决断（接受现状 / 立即修复 / 停止该功能 / 延后处理）并记录理由。
- **怎么用**：根据业务紧急度与风险承受力做取舍，白纸黑字写明决断理由与签字。
- **避坑红线**：严禁默认全部接受或交由 AI 自动决定，每个选项背后都对应着明确的业务责任。

**兜底降级与责任归属（Fallback）**：
- **是什么**：当 AI 接口超时、算不出或置信度低时，系统自动切入备用规则或人工处理的保底方案。
- **怎么用**：设计 3 种标准降级路径（规则兜底 / 缓存兜底 / 人工兜底），并在界面明确打上降级标签与第一责任人。
- **避坑红线**：严禁“静默失败”或用假数据伪装成“AI 实时计算成功”欺瞒用户与下游。

**原型 ≠ 生产系统（样板间不是交付住房）**：
- **是什么**：原型只用于验证业务逻辑是否跑得通、场景值不值得做；生产系统还需通过 IT 的安全审计与运维保障。
- **怎么用**：拿原型去跟领导要资源、跟 IT 提需求，作为立项前最直观的说服工具。
- **避坑红线**：严禁拿着跑通的原型直接要求 IT“明天上线”，原型不能替代正式的系统架构与压力测试。

**风险红线与非目标**：
- **是什么**：明确圈定不能自动执行的高危动作（如打款、签合同）、高敏感数据与本次坚决不做的事项。
- **怎么用**：写入交接清单的首要位置，让 IT 团队和合规部门一眼看清安全边界。
- **避坑红线**：严禁隐藏已知风险，不能把未解决的疑难问题包装成已完成功能进行交接。

**产品决策（继续 / 调整 / 暂缓 / 停止）**：
- **是什么**：综合考虑投入价值、已有证据、潜在风险与落地成本后，对项目前途作出的理智决断。
- **怎么用**：该投的全力推进，证据不足的调整方向，风险过高的坚决叫停；叫停也是高价值决策。
- **避坑红线**：严禁因为“花精力做出来了”就产生沉没成本执念、盲目强推上线。

**工程落地差距清单（生产就绪缺口）**：
- **是什么**：诚实列出原型与生产系统之间的真实差距（如缺少真实接口、未做权限隔离、缺少高并发测试等）。
- **怎么用**：作为交给 IT 团队的正式输入清单，让工程师清楚知道接下去具体要做哪些落地改造。
- **避坑红线**：严禁将原型缺陷清单隐瞒不报，交接不诚实会导致后续工程严重延期或翻车。

**责任交接（业务/数据/模型/审批/门禁）**：
- **是什么**：把业务、数据、模型、审批与上线门禁的责任明确移交给具体部门或个人的交接动作。
- **怎么用**：为清单中的每一项指定清晰的 Owner（负责人），未确定的标为“待定”并指定跟进人。
- **避坑红线**：严禁一句“交给 IT 了”模糊带过；交接是责任的清晰转移，绝不是责任消失。

**开发启动包（Development Kick-off Pack）**：
- **是什么**：汇总了业务问题、原型证据、契约规范、AI 选型、风险红线、落地差距与产品决策的完整交付资产包。
- **怎么用**：在 5 分钟立项汇报中完整呈现，作为 IT 正式立项与系统研发的高质量设计输入。
- **避坑红线**：严禁交付零散没有组织的代码文件，必须包含完整的决策理由与验证依据。
