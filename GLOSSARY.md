# 主管 AI 原型制作训练营 术语词典（GLOSSARY）

本词典收录本教学工作区统一遵循的工程与业务概念，与已冻结的 `docs/COURSE_ROADMAP.md` 与 `docs/LESSON_TEMPLATE.md` 保持同一术语口径。每个词条包含四要素：是什么、不是什么（_Avoid_）、实操对应点、主管价值。

## 一、架构与工程概念（Architecture & Engineering）

**大语言模型（LLM）**：
- **是什么**：基于 Transformer 架构、在内存中按概率推演序列的语言预测模型，本身不直接读写操作系统文件或调用外部工具。
- **不是什么**（_Avoid_）：全知全能的 AI、智能黑盒、能自行操作电脑的实体。
- **实操对应点**：L1 起学员通过 Coding Agent 间接使用 LLM；教师在第 9 课受控环境中演示真实模型调用。
- **主管价值**：知道"生成内容来自概率模型"而非确定计算，因此需要人在回路确认与证据核验。

**工具接口（Tools）**：
- **是什么**：由宿主程序暴露给 Agent 的受控接口（如文件读写、终端运行、浏览器操作），是 Agent 操作环境的通道。
- **不是什么**（_Avoid_）：AI 自发拥有的功能、插件黑盒。
- **实操对应点**：L1 工具权限沙箱；L7 教师预配置的单一浏览器验收入口（后台可组合多个工具，包括 MCP 工具）。
- **主管价值**：知道工具是"被授予的权限"，授权范围与沙箱边界由主管把关。

**受控执行者（Agent）**：
- **是什么**：以 LLM 为核心控制器、提示词与规则为约束、Tools 为操作通道，在明确目标与边界下组织多步行动的软件实体。
- **不是什么**（_Avoid_）：自动化脚本、机器人、能自行决策的实体。
- **实操对应点**：L1 受控 Agent 原型体验；L8 开发 Agent 与独立审查 Agent 双轨协作。
- **主管价值**：知道 Agent 的"受控"来自目标、边界、工具与停止条件，而不是 Agent 自身可靠；关键动作仍需 HITL 确认。

**ReAct 范式**：
- **是什么**：Reasoning and Acting 闭环——Agent 每次行动后，将观察结果（工具返回值、测试日志）送回 LLM 进行下一步推理与自修正。
- **不是什么**（_Avoid_）：盲目试错、自动改错、无监督的自我修复。
- **实操对应点**：L1 以纯语言体验"模型提出下一步—工具执行—主管确认"；本课程中 ReAct 为识别级概念，不要求学员独立实现。
- **主管价值**：知道 Agent 的"思考—行动—观察"循环需要人在关键节点确认，循环本身不保证正确。

**人在回路授权门禁（HITL）**：
- **是什么**：在 Agent 修改代码、写盘或发布前暂停自动化，交由人类主管确认的流程性授权环节。
- **不是什么**（_Avoid_）：人工客服、机器弹窗、运行时硬控制。
- **实操对应点**：L1 主管确认动作；L8 主管依据 CEO 3 大决策树行使一票裁决。
- **主管价值**：知道 HITL 是"流程要求人确认"，与"系统强制拦截"（运行时硬控制）属于不同控制层。

**模拟数据（Mock Data）**：
- **是什么**：符合业务结构契约但内容完全虚构的数据，用于原型演示与降级渲染。
- **不是什么**（_Avoid_）：真实测试数据、生产数据、未脱敏的真实数据。
- **实操对应点**：L1 起学员原型全部使用 Mock；L9–L10 真实 API 失败时降级回 Mock 渲染并显示降级来源。
- **主管价值**：知道"页面在跑但数据是假的"，验收时须区分 Mock 与真实数据来源。

**试衣镜（127.0.0.1）**：
- **是什么**：运行于本地开发服务器的前端预览窗口，用于实时肉眼校验页面外观与交互。
- **不是什么**（_Avoid_）：线上生产环境、测试服务器。
- **实操对应点**：L2 视觉重构前后对比；每课课堂内可见成果的肉眼校验。
- **主管价值**：知道本地预览只代表"本机跑得起来"，不代表生产可用。

**设计令牌（Design Token）**：
- **是什么**：在设计规范文件（`DESIGN.md`）中事先约定的标准化视觉变量（主色、字号标尺、间距、圆角）。
- **不是什么**（_Avoid_）：随机内联样式、临时配色。
- **实操对应点**：L2 按规程点开 `DESIGN.md` 映射 Token 与组件。
- **主管价值**：知道视觉一致性来自"事先约定并遵守规范"，而非口头审美要求。

**视觉 Harness（DESIGN.md）**：
- **是什么**：为 Agent 设立的视觉约束规范，要求写样式前先查阅并锚定设计字典。
- **不是什么**（_Avoid_）：口头审美要求、提示词自由发挥、运行时强制拦截。
- **实操对应点**：L2 视觉 Harness 重构；模板 9a 双轨教学中的教师演示。
- **主管价值**：知道视觉约束是"指导性约束"，靠流程与检查执行，不是系统强制。

**追问范式（Grill-Me）**：
- **是什么**：使用反向面试的思考路径，让 Agent 主动扮演考官，追问并锁定目标、边界、风险、停止条件等要素。
- **不是什么**（_Avoid_）：模糊需求、凭空脑补、一次问多个问题。
- **实操对应点**：L3 使用 `grill-me` Skill 进行 3–5 分支、5–7 轮追问。
- **主管价值**：知道"问清楚再动手"是降低返工的关键，主管的确认是需求冻结的前提。

**数据契约（Data Contract）**：
- **是什么**：在写界面代码前落盘的标准化数据结构约定（字段、类型、枚举、范围），确保 UI 与未来 IT API 对接有共同语言。
- **不是什么**（_Avoid_）：随机内联字段、临时假数据、TypeScript 类型本身。
- **实操对应点**：L3 在 `docs/BUSINESS_FEATURE_CARD.md` 中嵌入数据契约表，Agent 生成 TS 类型与 Mock 数据。
- **主管价值**：知道数据契约是"业务字段的书面约定"，主管核对业务合理性后冻结。

**工作区/工作树（Working Tree）**：
- **是什么**：开发者或 AI 正在修改代码的文件夹区域，与 Git 版本历史配合记录改动。类似于厨师切菜的砧板，但 Git 工作区有版本历史，砧板没有。
- **不是什么**（_Avoid_）：缓存目录、临时文件区、没有版本历史的普通文件夹。
- **实操对应点**：每课查看 `git diff` 确认修改范围；L2 节点 1/2 存档。
- **主管价值**：知道"改动发生在哪、能回到哪个版本"，不需要手敲 Git 命令。

**增量实施范式（Plan & Execute）**：
- **是什么**：将大需求拆解为包含具体文件路径与状态的小切片，每次只授权完成一个可验证的小成功路径。
- **不是什么**（_Avoid_）：巨石盲开、一口气改全盘、无计划的连续修改。
- **实操对应点**：L4 生成 `docs/LESSON_04_IMPLEMENTATION_PLAN.md` 并经主管审批后分步执行。
- **主管价值**：知道"一次只做一小块、每块可验证"，降低失控风险。

**工程规约（CLAUDE.md）**：
- **是什么**：项目根目录下的 Agent 工作规则文件，约定包管理器、依赖禁令、安全边界与工作流程。
- **不是什么**（_Avoid_）：口头规则、临时说明文档、运行时硬控制。
- **实操对应点**：L5 工程 Harness；根 `CLAUDE.md` 为母仓库维护者/Agent 工作规则，学员版为 `student-package/templates/CLAUDE.md`。
- **主管价值**：知道 CLAUDE.md 是"指导性约束"，靠 Agent 遵守与流程检查，不是系统强制拦截。

**三分记忆模型（Three-Layer Memory Model）**：
- **是什么**：将项目记忆解耦为工作记忆（聊天窗口）、外部长期记忆（Markdown 状态文件与实施计划）与版本证据（Git Commit 节点）三层。
- **不是什么**（_Avoid_）：只靠聊天记录记忆、把聊天窗口当唯一记忆。
- **实操对应点**：L5 三分记忆模型；每课更新 `docs/PROJECT_STATE.md` 并提交稳定版本。
- **主管价值**：知道"项目状态写在文件里、版本证据存在 Git 里"，不依赖会话上下文。

**有界存档与恢复（Bounded Save & Recovery）**：
- **是什么**：通过 Git Commit 建立稳定存档点，在需要还原时由教师或助教按确认范围执行有界恢复。
- **不是什么**（_Avoid_）：无确认的自动撤销、自动恢复、学员手敲 Git 命令。
- **实操对应点**：L2 节点 1/2 存档；L6 两轮熔断后的恢复流程。（自动恢复脚本为待实现教学资产，当前不作为课堂通过条件。）
- **主管价值**：知道"出问题时谁负责恢复、恢复到哪个点"，恢复动作有确认范围。

**五层诊断卡（Five-Layer Diagnostic Map）**：
- **是什么**：将 Web 应用排错拆解为环境层、数据源层、组件状态层、日志层与契约断言层五层，逐层定位问题。
- **不是什么**（_Avoid_）：盲目试错、控制台恐慌、一次排查所有层。
- **实操对应点**：L6 按五层顺序排查"界面呈现与业务契约不一致"的问题。
- **主管价值**：知道排错有固定顺序与范围，主管不需要深入底层代码。

**事实锚定排错（Fact-Anchored Feedback Loop）**：
- **是什么**：修改代码前先建立可测的 PASS/FAIL 验证信号，脱敏日志并落盘记录，再按证据排错的受控范式。
- **不是什么**（_Avoid_）：直接复制敏感 Token 抓包、无断言盲改。
- **实操对应点**：L6 排错前建立验证信号，诊断报告落盘。
- **主管价值**：知道"先有证据再动手"，排错过程可复核。

**2 轮熔断机制（Two-Round Circuit Breaker）**：
- **是什么**：同一 Bug 最多由 Agent 自修 2 轮；超过两轮后停止自动修改，保留诊断证据，由教师或助教恢复到已确认基线。
- **不是什么**（_Avoid_）：盲目死循环修复、自动恢复、越改越崩。
- **实操对应点**：L6 排错护盾；L4 自测失败时的恢复流程。（自动恢复脚本为待实现教学资产，当前不作为课堂通过条件。）
- **主管价值**：知道"修不动就停"，停止后由人接管恢复，不无限消耗。

**QA 子智能体（QA Subagent）**：
- **是什么**：受主 Agent 调度的只读沙箱子进程，执行页面验收并输出证据，完成后释放沙箱。
- **不是什么**（_Avoid_）：口头凭空承诺、污染主开发上下文、可写主工作区的进程。
- **实操对应点**：L7 浏览器验收流程；每课规定的学员验证方式。
- **主管价值**：知道验收由独立子进程执行并落盘证据，结果可复核。

**双 MCP 工具链（Dual MCP Tooling）**：
- **是什么**：由浏览器自动化 MCP 与浏览器开发者工具 MCP 构成的多维测试工具链，用于页面交互与运行状态检查。
- **不是什么**（_Avoid_）：单一静态截图、忽略运行报错、学员自行配置的 MCP 服务。
- **实操对应点**：L7 教师预配置的单一浏览器验收入口，后台可组合多个工具；学员无需自行配置 MCP 服务或 API Key。与"MCP 最小模型"词条关联理解。
- **主管价值**：知道"工具链由教师预配置"，学员侧不接触 MCP 配置。

**四类证据链与验收总卡（Four-Category Evidence Chain & Index）**：
- **是什么**：由视觉截图、行为日志、工程检查、变更范围四类证据及验收总卡构成的客观审计证据集合。
- **不是什么**（_Avoid_）：口头验收、无落盘物证交付、把"文件存在"当业务通过。
- **实操对应点**：L7 落盘四类证据卡；L10 打包进《产品决策与开发启动包》。
- **主管价值**：知道验收以可复核证据为准，证据可追溯。

## 二、控制层三分（Control Layers）

**指导性约束（Guidance）**：
- **是什么**：文档、提示词、规则说明等靠人遵守的约束。
- **不是什么**（_Avoid_）：系统强制拦截、运行时硬控制。
- **实操对应点**：`CLAUDE.md`、`DEPARTMENT_REDLINES.md`、提示词模板。
- **主管价值**：知道哪些规则是"约定"而非"系统强制"，需要人执行与检查。

**工作流门禁（Workflow Gate）**：
- **是什么**：流程中的检查点与批准步骤，靠流程执行。
- **不是什么**（_Avoid_）：代码层面的强制拦截、自动批准。
- **实操对应点**：教案→批准→GUIDE 的单向顺序；Git Commit 前主管确认；L3 契约冻结。
- **主管价值**：知道哪些环节需要自己签字，签字即承担责任。

**运行时硬控制（Runtime Hard Control）**：
- **是什么**：代码或系统层面的强制拦截，靠程序保证。
- **不是什么**（_Avoid_）：文字规则、流程约定、口头要求。
- **实操对应点**：沙箱权限隔离、只读 references、工具权限边界。
- **主管价值**：知道哪些是"系统不让做"而非"规则不让做"，两者不能混称。

## 三、MCP 与 API 区分（MCP & API Distinction）

**MCP 最小模型（MCP Minimal Model）**：
- **是什么**：host/client/server/tool 四要素构成的工具接入协议层，描述"工具怎么接"。
- **不是什么**（_Avoid_）：产品前端调用后端的业务 API、模型 API、学员需自行配置的服务。
- **实操对应点**：L7 单一教师预配置浏览器验收入口；后台可组合多个工具。
- **主管价值**：知道"工具怎么接"与"业务怎么调"是两层，MCP 不是产品业务 API。

**开发工具链（Development Toolchain）**：
- **是什么**：学员/教师用于构建与验证的本地工具（如 npm、脚本、浏览器开发者工具）。
- **不是什么**（_Avoid_）：业务系统对外接口、模型供应商服务。
- **实操对应点**：L9 三链区分；每课规定的学员验证方式。
- **主管价值**：知道开发工具链服务于"开发与验证"，与业务/模型链路分开。

**业务 API（Business API）**：
- **是什么**：业务系统对外提供的接口，属于产品业务能力。
- **不是什么**（_Avoid_）：模型供应商接口、本地开发工具。
- **实操对应点**：L9 三链区分；L10 未来 IT 对接的真实后端接口。
- **主管价值**：知道业务 API 是"产品与业务服务"的连接，涉及真实数据与凭证。

**模型 API（Model API）**：
- **是什么**：大模型服务接口，属于模型供应商能力。
- **不是什么**（_Avoid_）：业务系统接口、本地工具。
- **实操对应点**：L9 教师受控环境中的真实模型调用；学员侧不配置 API Key。
- **主管价值**：知道模型 API 是"供应商能力"，调用涉及凭证与成本，须在受控环境进行。

**三链区分（Three-Chain Distinction）**：
- **是什么**：开发工具链、业务 API、模型 API 三条链分别描述调用者、凭证、数据、失败与 owner 的区分框架。
- **不是什么**（_Avoid_）：把三条链混称、把 MCP 与业务/模型链混称。
- **实操对应点**：L9 三链图；模板 14a 三链区分字段。
- **主管价值**：知道"谁在调、用什么凭证、数据到哪、失败归谁"，便于与 IT 沟通。

## 四、Git 概念（Git Concepts）

**提交节点（Git Commit）**：
- **是什么**：项目在某一时刻的存档点，包含完整历史，可回到任意已提交版本。
- **不是什么**（_Avoid_）：无历史的普通快照、业务审批记录。
- **实操对应点**：每课关键节点存档；L2 节点 1/2。
- **主管价值**：知道"存了什么、能回到哪里"，不需要懂 Git 语法。

**差异视图（Git Diff）**：
- **是什么**：两次提交之间或工作区与提交之间的红绿对比视图，显示改动范围。
- **不是什么**（_Avoid_）：代码评审本身、业务审批。
- **实操对应点**：每课查看 `git diff` 确认修改范围；L8 审查 Candidate Commit 的 Diff。
- **主管价值**：看懂改动范围，不需要懂语法。

**有界恢复（Bounded Recovery）**：
- **是什么**：由教师或助教按确认范围执行版本回退，学员不直接手敲 Git 命令。
- **不是什么**（_Avoid_）：无确认的自动撤销、自动恢复、学员自行执行破坏性命令。
- **实操对应点**：L2 需要还原时；L6 两轮熔断后的恢复流程。
- **主管价值**：知道"出问题时谁负责恢复、恢复到哪个点"。

## 五、Mock / Fallback / Degraded（Mock、降级与退化）

**Mock 降级保护桥梁（Mock Fallback Bridge）**：
- **是什么**：真实 API 失败或超时时，降低页面不可用风险，显示降级来源并回退到 Mock 渲染的机制。
- **不是什么**（_Avoid_）：假装真实数据、隐藏失败、无标记的降级。
- **实操对应点**：L9–L10 真实 API 失败/超时/非法返回时的降级路径。
- **主管价值**：知道"页面还在跑但数据是假的"，降级来源必须可见。

**降级状态可见（Degraded State Visibility）**：
- **是什么**：降级发生时界面必须明确标记数据来源，不假装真实数据。
- **不是什么**（_Avoid_）：静默降级、把 Mock 当真实数据展示。
- **实操对应点**：所有 Mock 与降级结果在界面上明确标记来源。
- **主管价值**：知道"看到的数据来自哪里"，避免误判。

## 六、审查与裁决（Review & Adjudication）

**独立审查（Independent Review）**：
- **是什么**：新建只读隔离会话，仅调取已确认需求、稳定 Commit 与 Candidate Commit 的版本变更进行审查。
- **不是什么**（_Avoid_）：在同一实现会话中改名审查、审查者修改代码。
- **实操对应点**：L8 Codex 独立审查；工具不可用时改用隔离新会话，不跳过审查。
- **主管价值**：知道"审查者拿什么、不拿什么"，审查结论独立于开发过程。

**证据抽验（Evidence Sampling）**：
- **是什么**：从四类证据中抽取关键项重新核验，不全量复跑。
- **不是什么**（_Avoid_）：重新做一遍全部验证、口头相信结论。
- **实操对应点**：L8 审查者重新抽验关键证据。
- **主管价值**：知道"审查不是重新做一遍"，抽验有重点。

**主管裁决（Supervisor Adjudication）**：
- **是什么**：基于审查结果与证据，主管作出 Keep/Reject/Rework 决定。
- **不是什么**（_Avoid_）：交给 Agent 自裁、无证据的拍板。
- **实操对应点**：L8 依据 CEO 3 大决策树行使一票裁决。
- **主管价值**：知道"最终决定权在我"，裁决基于证据与业务判断。

**CEO 3 大决策树（CEO Decision Tree）**：
- **是什么**：本课程主管处置框架，用于仲裁审查发现（业务优先/安全红线/范围防漂移）。
- **不是什么**（_Avoid_）：代码运行时硬控制、行业通用标准拓扑、自动决策系统。
- **实操对应点**：L8 双 Agent 辩论超过 2 轮时主管依据决策树裁决。
- **主管价值**：知道这是"处置框架"而非"系统规则"，最终判断由主管作出。

## 七、交接与就绪（Handoff & Readiness）

**产品决策与开发启动包（Product Decision & Development Kickoff Package）**：
- **是什么**：课程结业交付物，包含功能卡、TS 数据契约、四类测试证据链与原型源码，供 IT 开始正式设计与开发。
- **不是什么**（_Avoid_）：能上线的系统、生产就绪交付物、用 placeholder 填充的包。
- **实操对应点**：L10 在教师/助教确认范围内生成标准归档 ZIP 包。
- **主管价值**：明确这是给 IT 的"开工包"，不是上线系统。

**非生产就绪系统（Non-Production-Ready System）**：
- **是什么**：明确声明本课程所有产物是原型，供 IT 开始正式设计与开发，不直接上线。
- **不是什么**（_Avoid_）：生产系统、可对外服务的产品。
- **实操对应点**：L10 交付物标注；路线图核心定位。
- **主管价值**：知道"原型≠上线"，避免误用。

## 八、Agent 协作模式（Agent Collaboration Modes）

**六种 Agent 协作模式（Six Agent Collaboration Modes）**：
- **是什么**：调度者—执行者、顺序流水线、上下文隔离独立审查（三种深讲/演示）；并行扇出—汇总、对等协作、人机协作仲裁（三种识别/比较）。
- **不是什么**（_Avoid_）：行业唯一标准、要求学员实现六套系统、自动选型规则。
- **实操对应点**：L8 作为主管决策框架比较选型。
- **主管价值**：知道"选哪种协作方式"是主管决策，不是工程自动决定。

## 九、教学与设计核心术语（Pedagogy & Design Terms）

**四步概念解析公式（Four-Step Concept Formula）**：
- **是什么**：首次出现的必须掌握概念按"是什么/不是什么、机制、业务类比与反例、交接价值（回指刚完成的实操）"四步讲解。
- **不是什么**（_Avoid_）：只讲定义、比喻无边界说明、概念不接实操。
- **实操对应点**：每课首次出现的必须掌握概念，单概念 3–5 分钟。
- **主管价值**：确保概念不浮于表面，学完能用于与 IT 沟通。

**概念暴露台账（Concept Exposure Ledger）**：
- **是什么**：每课记录概念是首次出现还是再次迁移、学员侧命名/纯语言体验/教师背景、是否独立考核的台账。
- **不是什么**（_Avoid_）：把复合名称当一个概念、无负荷控制的随意命名。
- **实操对应点**：LESSON_TEMPLATE 3a 节；每课最多 2–3 个必须掌握概念。
- **主管价值**：控制每课概念负荷，知道"这课到底要掌握几个概念"。

**双轨教学（Dual-Track Teaching）**：
- **是什么**：教师统一工作台与学员个人原型两条轨道并行推进的教学方式。
- **不是什么**（_Avoid_）：学员每课重建项目、教师与学员共用同一项目。
- **实操对应点**：十课统一案例为"业务异常预警与闭环处置工作台"；学员从 L1 起持续发展个人部门原型。
- **主管价值**：知道"我跟什么、学员做什么"，两条轨道边界清晰。

**业务异常预警与闭环处置工作台（Business Alert & Closed-Loop Workstation）**：
- **是什么**：教师十课统一演示案例，覆盖看指标、看异常、下钻明细、做出决策的闭环。
- **不是什么**（_Avoid_）：学员个人原型、每课更换的演示案例。
- **实操对应点**：每课教师演示的正常路径、失败/降级、证据链、拒绝/停止。
- **主管价值**：统一案例降低认知切换成本，主管可对照理解本部门场景。

**TypeScript 类型与运行时 Schema 的区别**：
- **是什么**：TS 类型是编译时检查，运行时 Schema 是代码层面的数据结构校验，两者不等价。
- **不是什么**（_Avoid_）：把"类型对"当"运行时数据对"。
- **实操对应点**：L3 数据契约；L7 契约合规检查。
- **主管价值**：知道"类型对不代表运行时数据对"，验收需看运行时证据。
