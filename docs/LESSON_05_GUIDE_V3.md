# 第五课学员操作指南 (GUIDE V3 双模合一终极版) — 工程 Harness 护栏与记忆机制

> **📖 本指南支持双模式使用**：
> - **【课堂极速模式】**：上课时只需关注加粗的 **指令与操作步骤**，1 秒定位完成实操。
> - **【课后无痛自学模式】**：若未参加课堂听讲，请阅读每个任务下的 `💡 独立自学原理解析` 与 `🔍 代码/配置对比`，无需讲师也可 100% 独立看懂。

---

## 💡 IT 跨界沟通术语卡：Engineering Harness (工程鞍具 / 物理护栏)

- **标准 IT 术语**：`Engineering Harness` (工程鞍具 / 物理护栏)
- **生活化通俗比喻**：**赛马的鞍具与缰绳** 🏇
  - 没有鞍具和缰绳的野马（AI Agent），跑得虽快但随时可能把骑手摔下马，或者跑偏方向。
  - 给马儿戴上鞍具（`CLAUDE.md` + 构建禁令），骑手（主管）才能精准把控方向，确保 AI 不脱轨。
- **IT 沟通场景**：“我们为项目部署了工程 Harness 护栏，物理锁定了包管理器与编码规则，确保 AI 生成的代码安全可控。”

---

## 🧠 三分记忆模型结构图

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 长期记忆 (Long-term)  ➔ CLAUDE.md                        │
│    - 物理存放在磁盘根目录                                    │
│    - 记录包管理器禁令、构建脚本与 4 大业务防错红线          │
├─────────────────────────────────────────────────────────────┤
│ 2. 中期记忆 (Mid-term)    ➔ LESSON_04_IMPLEMENTATION_PLAN.md│
│    - 记录 Step 1..N 的状态流转与 commit_sha                 │
├─────────────────────────────────────────────────────────────┤
│ 3. 短期记忆 (Short-term)  ➔ 对话窗口 Context Buffer         │
│    - 随着对话增长会溢出与遗忘                                │
│    - 定期使用 /compact 指令提纯                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 课堂实操与自学导引任务清单

### Task 1: 使用原生 `/init` 指令生成基础护栏

#### ⚡ 极速操作步骤 (课堂看这里)
1. 打开 Claude Code CLI 窗口，输入原生初始化指令：
   ```text
   /init
   ```
2. AI 将自动扫描当前项目目录，并在根目录下生成基础的 `CLAUDE.md` 文件。

#### 💡 独立自学原理解析 (自学看这里)
> **原生 `/init` 指令的物理本质**  
> `/init` 是 Claude Code CLI 的内置原生 Slash Command。运行后，AI 会自动扫描工程中的 `package.json`、`tsconfig.json` 等文件，分析项目架构并自动生成 `CLAUDE.md` 框架。这避免了手动从头编写配置文件的繁琐。

---

### Task 2: 补充 4 大防错红线并盖章落盘

#### ⚡ 极速操作步骤 (课堂看这里)
1. 检查生成的 `CLAUDE.md`，要求 AI 补充以下 4 大物理防错红线：
   - 规则 1：**包管理器硬约束**：强制统一使用 `npm`，绝对禁止混用 `yarn` 或 `pnpm`。
   - 规则 2：**边界控制**：禁止修改未在 `allowed_files` 许可范围内的任何文件。
   - 规则 3：**代码规范**：优先使用 Early Return（早期返回），消除深层嵌套。
   - 规则 4：**异常捕获**：强制进行边界条件检查与 Error Handling。
2. 在聊天窗口下发盖章落盘口令：
   ```text
   同意保存 CLAUDE.md 项目护栏配置
   ```
3. 打开工程根目录下的 [`CLAUDE.md`](file:///d:/AILearning/CLAUDE.md)，确认 4 大红线已成功落盘。

#### 🔍 护栏配置结构预览 (自学看这里)
```markdown
# 项目工程护栏 (CLAUDE.md)

## 构建与测试命令
- 构建：npm run build
- 类型检查：npm run typecheck

## 4 大防错红线
1. [包管理器禁令] 强制统一使用 npm，绝对禁止使用 yarn / pnpm。
2. [范围禁令] 修改代码前必须对照 IMPLEMENTATION_PLAN 中的 allowed_files。
```

---

### Task 3: 上下文提纯与 S/L 存读档演练

#### ⚡ 极速操作步骤 (课堂看这里)
1. **测试 Harness 防护效果**：向 AI 提问：
   ```text
   我们可以用 yarn 安装一个新包吗？
   ```
   *预期回答*：AI 引用 `CLAUDE.md` 规则拒绝，并提示“根据项目 Harness 规定，禁止使用 yarn，必须统一使用 npm”。
2. **上下文提纯 (Compact)**：在 CLI 中输入：
   ```text
   /compact
   ```
   清理短期对话缓存，提纯核心上下文。
3. **S/L 存读档还原 (State Restore)**：关闭 CLI 窗口并重新打开，下发自然口令：
   ```text
   请读取 IMPLEMENTATION_PLAN 恢复当前 Step 状态
   ```
   *预期效果*：AI 在 1 秒内读取磁盘中期记忆，精准汇报“已恢复进度：当前处于 Step 2 READY 状态”。

#### 💡 独立自学原理解析 (自学看这里)
> **为什么对话多了 AI 会变傻（幻觉）？**  
> LLM 的上下文窗口 (Context Window) 是有限的。当对话达到数万 Token 时，注意力机制被分散，AI 就会忘记第 1 轮对话里订立的规则。使用 `/compact` 可以压缩清理过期对话，而 `CLAUDE.md` 作为磁盘硬文件，在每一轮对话开始时都会被物理重新挂载，保证 AI 永不遗忘红线。

---

## ❓ 常见问题与踩坑排除 (Debug Guide)

| 报错/异常现象 | 物理原因 | 解决与纠偏方案 |
| :--- | :--- | :--- |
| 输入 `/init` 提示未知指令 | 当前运行的环境不是 Claude Code CLI | 确认是否在支持原生 slash command 的终端中运行。 |
| AI 依然擅自使用了 yarn 安装依赖 | `CLAUDE.md` 未写在工程根目录下 | 检查物理路径，确保 `CLAUDE.md` 在项目根目录，而非子文件夹。 |

---

## 📝 巩固与退场测试题库

### 阶段 1：课堂 5 分钟退场测试 (Exit Ticket - 课堂盖章)
1. **[填空题]** 在 Claude Code CLI 中，用于自动一键扫描并生成 `CLAUDE.md` 项目护栏的基础指令是 ____________。
2. **[IT 沟通场景题]** 当你的团队中有不同成员使用不同的 AI 工具导致代码风格冲突时，你应该如何向他们介绍 `CLAUDE.md` 的作用？
   - **参考回答**：“我们在根目录配置了 `CLAUDE.md` 工程 Harness 护栏，它作为全局长期记忆，锁定了包管理器命令与 4 大编码红线，无论是谁唤醒 AI 助手都会物理执行统一规范。”

---

### 阶段 2：课后自学拓展复习题库 (Self-Study Extension)
3. **[原理思考题]** 简述短期记忆 (Context)、中期记忆 (实施计划) 与长期记忆 (CLAUDE.md) 的物理分工与协作关系。
   - **自学提示**：短期记忆负责当前实时对话交互；中期记忆记录 Step 级实施进度与 SHA 记录；长期记忆定义不可违背的全局规则与构建约束。
4. **[实操演练题]** 尝试在 `CLAUDE.md` 中增加一条属于你业务团队的专属约束（如“禁止使用内网未许可的第三方 Icon 库”），并向 AI 提问验证其是否能自动遵循该约束。
