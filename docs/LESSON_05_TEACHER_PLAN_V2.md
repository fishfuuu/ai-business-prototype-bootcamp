# 第五课教师备课与控场指南 — 建立不会轻易失控的项目：工程 Harness 护栏与记忆机制

> [!IMPORTANT]
> 本教案为 **8 大执教模块精简标准版**，按讲师执教动线深度整合，删除了原 22 章的碎片冗余。

---

## 一、 课程元数据与定位

### 1.1 课程元数据
- **课程名称**：AI 业务原型开发训练营·第五课
- **主讲主题**：建立不会轻易失控的项目：工程 Harness 护栏与三分记忆机制
- **授课对象**：非技术背景业务主管 / 产品经理 / 业务专家 (10–30人)
- **课时时长**：90 分钟 (极客控场 + 5 个固定 Pause Points)
- **授课模式**：18 分钟极客示范 ➔ 10 分钟概念核对 ➔ 45 分钟学员分步实操 ➔ 10 分钟验证归档
- **前置依赖**：学员已完成第四课并掌握受控薄切片与状态机归档机制。
- **教学验证状态**：PILOT_PENDING (草稿V2 / 待合入 / 待试讲)
- **课程负责人**：待指定

### 1.2 本课定位与背景痛点
- **在全套课程中的位置**：承接第四课《受控 Agent 循环与物理状态机》，为工程构建建立持久化防护墙与记忆治理机制。
- **解决的核心痛点**：对话超过 30 轮后，AI 产生“幻觉”与“规则遗忘”，擅自混用包管理器或修改越界核心代码。
- **核心突破口**：引入“工程 Harness 鞍具 (`CLAUDE.md`)”与“三分记忆模型”，物理锁死 Agent 的行为红线，并支持 S/L 存读档一秒恢复会话断点。

---

## 二、 逆向目标与四步概念卡

### 2.1 Bloom ABCD 学习目标
完成本课后，学员将能够：
1. **[Objective 1 - Apply (3)]**：在给定的项目工程目录下 (**C**)，学员 (**A**) 能运行原生 `/init` 命令行指令 (**B**)，秒级生成并配置包含 4 大防错红线的 `CLAUDE.md` 项目护栏 (**D**)。
2. **[Objective 2 - Analyze (4)]**：在面对对话增长引发的 Context 膨胀时 (**C**)，学员 (**A**) 能通过 `/compact` 提纯指令与物理挂载机制 (**B**)，彻底消除 AI 的包管理器误用与规则遗忘 (**D**)。
3. **[Objective 3 - Evaluate (5)]**：在跨会话或断电重开场景下 (**C**)，学员 (**A**) 能下发自然恢复指令 (**B**)，实现 1 秒内无缝读取磁盘状态机并恢复开发断点 (**D**)。

### 2.2 核心概念四步解析卡集

#### 💡 概念卡 1：Engineering Harness (工程鞍具 / 物理护栏)
1. **硬核工程定义**：约束 Agent 行为边界的物理配置文件、校验脚本与权限防火墙组合。
2. **底层运作机制**：在 Agent 修改代码前强制读取 Harness 规则，违规指令直接拦截。
3. **具象业务比喻**：**赛马的鞍具与缰绳** 🏇。
4. **IT 沟通场景**：“我们为项目部署了工程 Harness 护栏，物理锁定了包管理器与编码规则。”

#### 💡 概念卡 2：三分记忆模型 (3-Layer Memory Architecture)
1. **硬核工程定义**：将 Agent 记忆划分为“长期 (`CLAUDE.md`)”、“中期 (`IMPLEMENTATION_PLAN.md`)”与“短期 (Context Buffer)”的三层解耦架构。
2. **底层运作机制**：长期规则物理挂载 Prompt 顶部，中期进度落盘防丢，短期缓存使用 `/compact` 提纯。
3. **具象业务比喻**：**公司章程 + 盘点账本 + 电话记事本** 📝。
4. **IT 沟通场景**：“项目建立了三分记忆模型，有效防止了 AI 规则遗忘。”

#### 💡 概念卡 3：S/L 存读档状态恢复机制 (State Save / Load Restore)
1. **硬核工程定义**：通过读取磁盘持久化状态机文件无缝重建 LLM 注意力锚点并恢复断点任务的机制。
2. **底层运作机制**：重开终端后读取 `current_waiting_step` 恢复上下文。
3. **具象业务比喻**：**单机游戏的 S/L 存档与读档** 🎮。
4. **IT 沟通场景**：“我们支持 S/L 存读档状态恢复，跨会话开发时 1 秒无缝接续进度。”

---

## 三、 教学准备与沙箱隔离

- **代码仓库准备**：确保上一课 `LESSON_04_IMPLEMENTATION_PLAN.md` 存在且状态可读取。
- **环境检查命令**：
  ```powershell
  git status
  ```
- **示范区与实验区**：讲师示范窗口与学员实操窗口相互独立隔离。

---

## 四、 90分钟控场主线与 Pause Points

| 时间段 | 环节 | WHERETO | 教师动作与 Pause Points 提问 | 学员动作 |
| :--- | :--- | :--- | :--- | :--- |
| 00-08 分 | 成果展示 | **W** | 展示野生 AI 幻觉崩溃 vs Harness 物理护栏拦截。<br>**Pause Point 1**：“为什么不能把规则只写在聊天框里？” | 观看对齐交付物 |
| 08-25 分 | 极客示范 | **H & E** | 示范 `/init`、`CLAUDE.md` 红线配置与 S/L 恢复。<br>**Pause Point 2**：“`CLAUDE.md` 护栏存放在哪里？有何物理效果？”<br>**Pause Point 3**：“`/init` 指令的物理本质是什么？” | 记录关键指令 |
| 25-35 分 | 概念核对 | **R** | 提问核对 Harness 鞍具与三分记忆模型。<br>**Pause Point 4**：“`/compact` 指令有什么用？” | 口头回答卡片 |
| 35-80 分 | 学员实操 | **E & T** | 巡视指导，监控护栏测试与 S/L 恢复。<br>**Pause Point 5**：“断电重开后如何恢复进度？” | 分 Task 独立实操 |
| 80-90 分 | 总结验证 | **O** | 运行 `verify-project.ps1` 校验。 | 填退场卡，提交日志 |

---

## 五、 逐 Task 极客示范与巡视指导

### Task 1: 使用原生 `/init` 指令生成基础护栏
- **教师示范**：在 CLI 中下发 `/init` 指令，展示 AI 自动扫描目录并生成 `CLAUDE.md` 的全过程。
- **盖章口令**：`/init`
- **巡视 Check**：工程根目录下成功生成 `CLAUDE.md` 文件。

### Task 2: 补充 4 大防错红线并盖章落盘
- **教师示范**：为 `CLAUDE.md` 补充包管理器硬约束（禁用 yarn/pnpm）、边界控制、Early Return 与 Error Handling。
- **盖章口令**：`同意保存 CLAUDE.md 项目护栏配置`
- **巡视 Check**：核对落盘的 4 大防错红线条款。

### Task 3: 上下文提纯 (`/compact`) 与 S/L 存读档演练
- **教师示范**：向 AI 提问“能用 yarn 吗”，验证防护墙；运行 `/compact`；关闭终端后重启并唤醒“请读取 IMPLEMENTATION_PLAN 恢复当前 Step 状态”。
- **盖章口令**：`请读取 IMPLEMENTATION_PLAN 恢复当前 Step 状态`
- **巡视 Check**：学员提问 yarn 时能收到拒绝提示，且重开终端后能精准恢复 Step 进度。

---

## 六、 现场 Debug 预案与自动化校验

### 6.1 常见错误现场 Troubleshooting
- **现象 1**：`/init` 未识别 ➔ **预案**：确认运行在 Claude Code CLI 环境中。
- **现象 2**：`CLAUDE.md` 规则不生效 ➔ **预案**：确认文件位于项目根目录而非子目录。

### 6.2 自动化校验与证据提取
- **校验命令**：
  ```powershell
  powershell -ExecutionPolicy Bypass -File scripts/verify-project.ps1
  ```
- **预期证据**：显示 `Verification completed successfully.`。

---

## 七、 退场测试与课后拓展作业

### 7.1 课堂退场测试卡 (Exit Ticket)
精选 4 道考查 `/init` 指令、三分记忆模型、IT 沟通表达与 S/L 恢复机制的题目。

### 7.2 课后练习与巩固作业
指导学员为其团队的其他工程尝试配置 `CLAUDE.md` 物理护栏。

---

## 八、 教师备课质量自测 Checklist

- [ ] 100% 匹配 **8 大执教模块** 标题，包含 5 个固定 Pause Points。
- [ ] 包含了【四步概念卡】与 HITL 授权盖章口令。
- [ ] 包含了按 Task 深度聚合的示范、口令与巡视指导。
- [ ] 包含了 PowerShell 脚本自测通过逻辑。
